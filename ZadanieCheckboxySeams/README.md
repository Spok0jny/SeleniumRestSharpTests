# ZadanieCheckboxySeams

This project features Selenium WebDriver tests in C# focusing on checkbox element interaction and verification using NUnit assertions. It validates the state of specific checkbox input elements located via XPath on a web page.
