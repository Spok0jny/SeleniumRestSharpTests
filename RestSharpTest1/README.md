# RestSharpTest1

This project implements REST API automated tests in C# using RestSharp and NUnit against the JSONPlaceholder mock service. It validates CRUD operations including POST request creation, response deserialization, and GET assertion workflows.
